達人榜前台最終修正版 v3

本版針對「HTTP 401: JWT expired」修正。

修正內容：
1. 歷屆達人榜／competitions 公開查詢不再使用 visitor_access_token。
2. 查詢只使用 SUPABASE_ANON_KEY，避免 localStorage 的過期訪客 JWT 造成 401。
3. 保留原本 published=true 與 published_at 條件。
4. 不修改後台、Edge Function、SQL、config.js。
5. 已更新 app.js cache-busting 版本為 v3。

部署方式：
- 將 app.js、index.html、competitions.html 覆蓋目前網站的同名檔案。
- 不要替換原本 config.js。
- GitHub Pages 部署完成後使用 Ctrl+F5。
- 不需要重新跑 SQL，也不需要重新部署 Edge Function。
